server:
  port: ${PORTS_MICROSERVICES_DATA}

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    enabled: false

cloud-storage:
      path: "test_bucket_for_cmiyc"

spring:
  application:
    name: data
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${ROUTES_MICROSERVICES_AUTHORITY}/.well-known/jwks.json
